import * as THREE from 'three';

export function createChessboard() {
    // const group = new THREE.Group();

    // const geometry = new THREE.PlaneGeometry(1, 1);
    // const materialWhite = new THREE.MeshBasicMaterial({ color: 0xFFFFFF });
    // const materialBlack = new THREE.MeshBasicMaterial({ color: 0x000000 });

    // for (let i = 0; i < 8; i++) {
    //     for (let j = 0; j < 8; j++) {
    //         const square = new THREE.Mesh(geometry, (i + j) % 2 === 0 ? materialWhite : materialBlack);
    //         square.position.set(i - 3.5, j - 3.5, 0);
    //         group.add(square);
    //     }
    // }
    // group.rotation.x = -Math.PI / 2;


    return group;
}